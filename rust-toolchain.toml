# ビルド結果が版数の更新で変わらないよう固定する
# 直下に置くのは、src-tauri と tools/ の両方に効かせるためである
# （doc/DEVELOPMENT.md 第 2 節）
[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
targets = ["x86_64-pc-windows-msvc"]
