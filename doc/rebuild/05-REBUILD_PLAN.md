# 再作成の進め方

## 1. この文書の位置づけ

[01-STACK_REVIEW.md](01-STACK_REVIEW.md) の決定と [02-SPECIFICATION.md](02-SPECIFICATION.md) の仕様をもとに、どの順で作り、どこで判断し、何をもって完了とするかを定める。

進め方の原則を 3 つ置く。

- 判断に必要な情報を早く得るため、不確実性の高い部分から着手する
- 各フェーズは、それ単独で動作を確認できる単位にする
- 捨てる C# のテストが見ていた観点を、Rust のテストとして先に置き直す

3 番目が今回の要である。
言語を変えることで、仕様を記録していた 121 個のテストを失う。
これを埋めないまま画面を作り始めると、何が動いていたのかを確かめる手段がなくなる。

## 2. フェーズ

### フェーズ 0：計測の基準を取る

再作成後と比較するための現行値を取る。

- 現行版で第 5 節の計測手順を実施し、結果を記録する
- 現行版でエクスポートを実行し、移行の検証に使う JSON を保存しておく

完了条件：起動時間、ホットキー応答、検索応答、常駐メモリの現行値が記録されている。

### フェーズ 1：実現性の確認

Tauri v2 でこの種のアプリが成立するかを、最小構成で確かめる。
画面は仮のもので構わない。

- ウィンドウを出し、Mica を適用する
- トレイアイコンと、グローバルホットキーによる表示切り替えを動かす
- エクスプローラからファイルを落とし、パスを受け取る
- 一覧の並び替えを、ポインタイベントで動かす（第 9 節の制約の確認）
- 入力欄に日本語を入力し、変換候補の表示位置を確認する
- release ビルドの実行ファイルの大きさと、常駐時のメモリ合計を測る

完了条件：[01-STACK_REVIEW.md](01-STACK_REVIEW.md) 第 10 節の 6 項目を全て満たす。
メモリが目標を超えた場合は、同 8.1 節の対策を入れて再計測する。

このフェーズを最初に置くのは、ここで得る答えが以降の設計全体を左右するからである。

### フェーズ 2：ドメインとユースケースの移植

Rust でドメインとユースケースを実装し、同時にテストを書く。
この段階では画面も永続化もなく、ポートは試験用の実装で差し替える。

移植する観点を第 3 節に一覧で示す。

完了条件：第 3 節の 21 領域すべてにテストがあり、通っている。

**結果（2026-09-08）**：完了した。
実装は `src-tauri/`（3,615 行、うちテストを含む）に置いた。

| 領域                                  | 場所                       | テスト |
| ------------------------------------- | -------------------------- | ------ |
| ドメイン（Item、Shelf、識別子、種別） | `src/domain.rs`            | 11 件  |
| Shelf のユースケース                  | `src/usecases/shelves.rs`  | 16 件  |
| Item のユースケース                   | `src/usecases/items.rs`    | 21 件  |
| 起動と親フォルダ                      | `src/usecases/launch.rs`   | 8 件   |
| 検索                                  | `src/usecases/search.rs`   | 21 件  |
| エクスポートとインポート              | `src/usecases/transfer.rs` | 17 件  |
| 合計                                  |                            | 94 件  |

`cargo test` が全て通り、`cargo clippy --all-targets` の警告は 0、`cargo fmt --check` も通る。

ポートは同期の trait とし、`&self` で受ける形にした（[03-ARCHITECTURE.md](03-ARCHITECTURE.md) 第 4.3 節）。
試験用の実装は `src/testing.rs` に置き、`testing` フィーチャで crate の外からも使えるようにしてある。

**フェーズの順序からの逸脱**：[02-SPECIFICATION.md](02-SPECIFICATION.md) 第 12 節の 1 番、2 番、7 番をこの段階で実装した。
いずれもドメインとユースケースの内側の変更であり、現行の振る舞いを一度作ってから直すより、最初から目的の形で書く方が短いためである。
フェーズ 6 に残るのは、画面側の 3 番から 6 番になる。

**移植中に見つけたこと**：検索のプレフィックス解析で、トークンの先頭が多バイト文字のときに文字の途中で切って異常終了する欠陥を作り込み、テストで検出した。
日本語で検索すれば必ず踏むもので、テストを先に置く方針がそのまま効いた。

### フェーズ 3：永続化

- JSON スナップショットのストアを実装する（読み込み、索引の構築、遅延保存、原子的置き換え、破損回復）
- ポートの実装として差し込む
- 読み書き、置き換え、破損ファイルからの回復をテストで検証する

完了条件：フェーズ 2 のテストが、実ファイルを使う構成でも通る。

**結果（2026-09-09）**：完了した。
実装は `src-tauri/src/adapters/store.rs` に置いた。

| 内容                       | テスト                                 |
| -------------------------- | -------------------------------------- |
| ストアの単体テスト         | 14 件（`src/adapters/store.rs`）       |
| 実ファイルでのユースケース | 6 件（`tests/usecases_over_files.rs`） |
| 合計                       | 114 件（フェーズ 2 の 94 件を含む）    |

確かめたことを挙げる。

- 保存して読み直すと、階層、並び順、ピン留め、メモ、最終アクセス日時、設定が残る
- 本体が壊れていれば控えから回復する
- 本体も控えも読めなければ、`shelfy.json.corrupt.<日時>` へ退避して空で始める
- 読めないレコードが 1 件混じっても、残りは読み込む
- 自分が知らない新しい版数のファイルは、読み取り専用として扱い、上書きしない
- 待ち時間が過ぎるまで書かず、過ぎたら 1 回だけ書く
- 保存前に終了した分だけが失われ、保存済みの内容は残る

`JsonStore` は `ShelfRepository`、`ItemRepository`、`SettingsRepository` の 3 つを兼ねる。
排他はミューテックス 1 つで行い、Tauri の管理状態にそのまま置ける形にした。

**設計からの変更**：派生した索引（親から子、Shelf から Item）は持たず、走査で求めることにした。
更新のたびに整合を保つ手間が、数千件の走査（数ミリ秒）に見合わないためである。
[03-ARCHITECTURE.md](03-ARCHITECTURE.md) 第 7.2 節を実装に合わせて直した。

### フェーズ 4：IPC と画面の骨格

- コマンドとイベントの境界を定義する（[03-ARCHITECTURE.md](03-ARCHITECTURE.md) 第 6 節）
- Shelf ツリー、Item 一覧、検索、起動までを通す
- 表示モード（通常、検索、最近、欠損）の切り替えを実装する
- ホットキーによる表示と非表示、トレイからの表示と終了を通す
- Fluent 風のスタイル基盤（色トークン、フォント、アイコン、角丸）を用意する

完了条件：フェーズ 0 で保存した JSON を取り込んだ状態で、検索して起動するまでの一連の操作ができる。

**結果（2026-09-10）**：完了した。動くアプリになった。

| 指標             | 実測                              | 目標          |
| ---------------- | --------------------------------- | ------------- |
| 単一 exe         | 3,396,096 バイト（3.24 MiB）      | 10 MB 以下    |
| フロントエンド   | 65,703 バイト（HTML、CSS、JS 計） | ---           |
| 起動から表示まで | 312 ms（295〜358、外部計測）      | 1,000 ms 以下 |
| 内部計測の起動   | 138 ms                            | ---           |
| 常駐メモリ       | プライベート WS 74.8 MB           | 100 MB 以下   |
| テスト           | 130 件（単体 124、結合 6）        | ---           |

画面から確かめたことを挙げる。

- Shelf の階層、ピン留め、Fluent アイコン、日本語がすべて描画される
- `report` と打つと 2 件が出て、所属 Shelf 名が併記される
- 「欠損」で参照先の無い 3 件が警告付きで出る
- 一覧で `Enter` を押すとメモ帳が開き、ウィンドウが自動で隠れる
- `Ctrl+Shift+Space` で表示が戻る
- 起動で更新された最終アクセス日時が保存され、読み直しても残る
- Mica が適用される（ログに記録される）

**フェーズ 0 の代わり**：現行版からのエクスポート（フェーズ 0）は、C# 実装を削除した時点で実施できなくなった。
代わりに、同じ交換形式のデータ（5 棚、9 項目、実在するもの 3 件と欠損 3 件）を組み立てて確認に使った。
利用者の実データは `SHELFY_DATA_DIR` で置き場所を差し替えて避けている。

**フェーズの順序からの逸脱**：[02-SPECIFICATION.md](02-SPECIFICATION.md) 第 12 節の 5 番（検索中に Shelf を選ぶと通常モードに戻る）を、この段階で実装した。
壊れた振る舞いを一度作ってから直す理由がないためである。
フェーズ 6 に残るのは 3 番、4 番、6 番になる。

**まだ無いもの**：作成、改名、削除、移動、並び替え、メモ編集、ドラッグアンドドロップ、ダイアログ、設定画面、エクスポートとインポートはフェーズ 5 で作る。

### フェーズ 5：操作の網羅

- Shelf と Item の作成、名前変更、削除、移動、並び替え、ピン留め、メモ編集
- コンテキストメニューとキーボードショートカット
- ドラッグアンドドロップ（エクスプローラからの追加、一覧内の並び替え、ツリー内の並び替え）
- ダイアログ（名前入力、メモ編集、Shelf 選択、確認、設定）
- エクスポートとインポート

完了条件：第 4 節の受け入れ確認のうち、仕様変更分を除く全項目が通る。

**結果（2026-09-10）**：実装を終えた。手元での確認が残っている。

| 指標           | 実測                              | 目標       |
| -------------- | --------------------------------- | ---------- |
| 単一 exe       | 3,620,352 バイト（3.45 MiB）      | 10 MB 以下 |
| フロントエンド | 90,839 バイト（HTML、CSS、JS 計） | ---        |
| テスト         | 130 件（変わらず）                | ---        |

足したものを挙げる。

- 棚の作成、名前変更、移動、削除、ピン留め、並び替え
- 項目の追加（エクスプローラからのドロップ、URL）、名前変更、メモ、別の棚への移動、並び替え、削除
- 棚と項目のコンテキストメニュー
- ダイアログ（名前入力、メモ、棚の選択、確認、設定）
- キー操作（`Ctrl+N`、`Ctrl+R`、`F2`、`Delete`、`Enter`、`Esc`、上下）
- エクスポートとインポート（ファイル選択は Tauri のダイアログ）
- 設定の保存と、ホットキーの入れ替え、ウィンドウの大きさの記憶

画面から確かめたことを挙げる。

- 棚の右クリックで、ピン留め済みなら「外す」と出て、先頭なら「上へ」が無効になる
- 「この中に棚を作る」で子の棚ができ、木に入れ子で現れる
- URL の追加が 2 段のダイアログで通り、種別が URL として保存される
- 項目の右クリックで、URL なら「親フォルダを開く」が無効になる
- 設定のダイアログが開き、ホットキーと大きさと件数を編集できる
- 作った棚と項目が、保存ファイルに残る

**残っている手動確認**：エクスプローラからのドロップ、ポインタでの並び替え、エクスポートとインポート、ホットキーの入れ替え、ウィンドウの大きさの記憶。
いずれも OS との対話や実際の操作が要るため、第 4 節の一覧で確かめる。

**まだ無いもの**：棚の並び替えをドラッグで行う操作。
コンテキストメニューの「上へ」「下へ」で代用できるため、フェーズ 7 の作り込みに合わせて判断する。

### フェーズ 6：仕様変更分の実装

[02-SPECIFICATION.md](02-SPECIFICATION.md) 第 12 節の 1 番から 6 番を実装し、それぞれにテストまたは確認手順を対応させる。

完了条件：変更した 6 項目が受け入れ確認で確かめられている。

**結果（2026-09-10）**：完了した。

先の段階で 1、2、4、5、6、7 を入れてあったため、この段階の作業は 3 番だった。
どこで入れたかは [02-SPECIFICATION.md](02-SPECIFICATION.md) 第 12.1 節に記録した。

**3 番（存在確認）の作り**：一覧はまず出し、参照先の確認は別のスレッドで進めて、
判明した分から 25 件ずつ画面へ送る（`existence-updated`）。
同じ参照先の結果は 30 秒だけ覚えておき、聞き直さない。

覚え書きは `adapters/existence.rs` に置き、単体テスト 5 件で確かめた。
取り込みで参照先の顔ぶれが変わったときは、覚えている結果を捨てる。

これにより、切断されたネットワークドライブを指す参照が 1 件混ざっても、一覧の表示は待たされない。

**確かめたこと**：

- 通常の棚の表示でも、参照先の無い項目に警告の印が付く（従来は欠損の一覧でしか出なかった）
- 2 つ目を起動してもプロセスは 1 つのままで、隠れていた既存のウィンドウが出る（6 番）

| 指標     | 実測                         |
| -------- | ---------------------------- |
| 単一 exe | 3,627,520 バイト（3.46 MiB） |
| テスト   | 135 件（単体 129、結合 6）   |

### フェーズ 7：性能の作り込み

- 非表示中のアニメーションとタイマーの停止
- 存在確認の背景実行とイベントでの返却
- 一覧の仮想スクロール
- 起動時のウィンドウ事前生成
- ビルド設定によるサイズ最適化

完了条件：第 5 節の計測で、[02-SPECIFICATION.md](02-SPECIFICATION.md) 第 11 節の目標値を全て満たす。

**結果（2026-09-10）**：完了した。目標をすべて満たした。

棚 1 個に項目 1,000 件（想定の上限）を入れた状態で、10 回ずつ測った。

| 指標               | 目標          | 実測                              | 余裕   |
| ------------------ | ------------- | --------------------------------- | ------ |
| 単一 exe           | 10 MB 以下    | 3,628,544 バイト（3.46 MiB）      | 2.9 倍 |
| zip                | 5 MB 以下     | 1,523,549 バイト（1.45 MB）       | 3.4 倍 |
| 起動から表示       | 1,000 ms 以下 | 中央値 310 ms、最大 452 ms        | 3.2 倍 |
| ホットキーから表示 | 100 ms 以下   | **中央値 1.4 ms**、最大 3 ms      | 71 倍  |
| 1,000 件での検索   | 50 ms 以下    | **364 µs**（最も重いクエリ）      | 137 倍 |
| 常駐メモリ         | 100 MB 以下   | プライベート WS 80.8 MB（非表示） | ---    |
| 非表示中の CPU     | 0% と見なせる | 90 秒で 0.094 秒（0.10%）         | 後述   |

参考の値も記す。

| 指標                       | 実測                         |
| -------------------------- | ---------------------------- |
| 1,000 件の保存             | 2.27 ms（314 KB）            |
| 1,000 件の読み込み         | 5.13 ms                      |
| ワーキングセット（非表示） | 376.0 MB（7 プロセスの合計） |

**この段階で作り込んだこと**

一覧の仮想スクロールを入れた。
1,000 件を素直に並べると描画に 73 ms かかり、棚を切り替えたときに引っかかる。
見えている範囲だけを描くようにして **11 ms** になった。

行の高さはメモの有無で 2 種類しかないため、累積和と二分探索で正確な位置が出せる。
高さを見積もらずに済むので、スクロール位置がずれない。

**非表示中の CPU の内訳**

プロセスごとに 60 秒測ったところ、内訳は次のとおりだった。

| プロセス                     | 60 秒での CPU 時間 |
| ---------------------------- | ------------------ |
| `shelfy`（自前）             | **0.000 秒**       |
| `msedgewebview2`（描画元）   | 0.172 秒           |
| `msedgewebview2`（その他 5） | 0.032 秒           |

自前のコードは測れるほど動いていない。
残りは WebView2 自身の背景動作であり、描画エンジンを止めない限り消せない。
1 コアの 0.3% ほどで、実用上は無視できる水準にある。

目標の「0% と見なせる水準」は、自前の側では満たしている。
値そのものは 0 ではないため、実測を残しておく。

**手を入れなかったこと**

ビルド設定はフェーズ 2 で入れたまま（`opt-level = "s"`、LTO、`codegen-units = 1`、`panic = "abort"`、`strip`）とした。
配布サイズが目標の 3 分の 1 に収まっており、これ以上削る理由がない。

### フェーズ 8：配布

- ワークフローの更新（第 6 節）
- WebView2 が無い環境での案内の確認
- 移行手順の検証（[04-DATA_MIGRATION.md](04-DATA_MIGRATION.md) 第 6 節）
- 現行の C# 実装にタグを打ち、`main` から削除する
- README とドキュメントの更新

完了条件：タグの push でリリース資産が生成され、置くだけで動く。

**結果（2026-09-11）**：完了した。

**ワークフロー**：`ci.yml` はバックエンド（書式、clippy、テスト）とフロントエンド（型検査、ビルド）を通し、
別の仕事として依存の監査（`cargo audit` と `npm audit`）を回す。
`release.yml` は `v*` タグの push で走り、テストのうえで実行ファイルを作り、
**10 MB を超えたら失敗する**ようにした。依存を足したときにサイズが静かに戻るのを防ぐためである。

同じ手順を手元でも通した（テスト 141 件、脆弱性 0）。
ワークフロー自体は GitHub 上でしか動かせないため、実際の実行は未確認である。

**WebView2 が無い環境の案内**：検出を一時的に失敗させて確かめた。
OS 標準の案内が出て、何が足りないかと入手先を伝えたうえで終了する。黙って落ちない。

**移行**：`tools/shelfy-migrate` を作り、実在する v1.0.0 のデータ（棚 5、項目 10）で通した。
元の `shelfy.db` は読み取り専用で開き、更新時刻が変わらないことも確かめた。

書き出した JSON と元の DB を照合し、[04-DATA_MIGRATION.md](04-DATA_MIGRATION.md) 第 6 節の観点がすべて一致した。

| 観点                   | 結果 |
| ---------------------- | ---- |
| 件数                   | 一致 |
| 識別子                 | 一致 |
| 階層（親子）           | 一致 |
| ピン留め               | 一致 |
| 種別                   | 一致 |
| メモの有無             | 一致 |
| 最終アクセス日時の有無 | 一致 |

道具の出力を取り込めることは、`tests/migration.rs` の 2 件でも固定した。

**旧実装の削除**：2026-09-10 に済ませた。タグ `v1.0.0` が削除前を指している。

## 3. 移植するテストの観点

現行の C# テストが見ていた領域を示す。
フェーズ 2 では、この 21 領域それぞれに対応する Rust のテストを置く。

| 領域               | 主に確かめること                                                         |
| ------------------ | ------------------------------------------------------------------------ |
| Item の生成と変更  | 空の参照先と表示名を拒む、改名、メモ更新、アクセス日時、並び順、所属変更 |
| Shelf の生成と変更 | 空の名前を拒む、改名、親の付け替え、並び順、ピン留めの反転               |
| 識別子と種別       | 等価性、文字列表現、種別の数値対応                                       |
| CreateShelf        | 入力検査、親の存在確認、兄弟の最大値 + 1 の並び順                        |
| RenameShelf        | 入力検査、対象の存在確認                                                 |
| MoveShelf          | 自分自身への移動の拒否、子孫への移動の拒否、ルートへの移動               |
| DeleteShelf        | 配下の再帰削除、所属 Item の削除                                         |
| TogglePinShelf     | 反転と結果の返却                                                         |
| ReorderShelves     | 0 から始まる連番の割り当て、空入力、存在しない識別子                     |
| AddItem            | 入力検査、Shelf の存在確認、重複判定の大文字小文字規則、並び順           |
| RemoveItem         | 参照だけを消すこと、対象の存在確認                                       |
| RenameItem         | 入力検査                                                                 |
| UpdateItemMemo     | 未設定への更新を含む                                                     |
| MoveItemToShelf    | 移動先の存在確認、移動先での重複判定、並び順の付け替え                   |
| ReorderItems       | ReorderShelves と同じ規則                                                |
| LaunchItem         | 対象の存在確認、失敗時の扱い、アクセス日時の更新、起動後の画面制御       |
| OpenParentFolder   | URL の非対応、対象の存在確認、失敗時の扱い                               |
| GetRecentItems     | 未アクセスの除外、降順、件数の上限                                       |
| GetMissingItems    | 存在しない参照の抽出、URL の扱い                                         |
| 検索               | クエリの解析、4 つの照合対象、3 種の絞り込み、空クエリ、不正な値         |
| Export と Import   | 書式の往復、全置換とマージ、除外規則                                     |

「起動後の画面制御」は、ホットキーの修飾キーが押されたままならウィンドウを残す規則を指す。
現行実装ではこれをユースケースの責務としており、その配置を引き継ぐ。

## 4. 受け入れ確認

自動テストで担保できない項目を、手動で確認する。

### 4.1 常駐と呼び出し

- [ ] 起動するとトレイに常駐する
- [ ] `Ctrl+Shift+Space` で表示と非表示が切り替わる
- [ ] 表示時にウィンドウが前面に出て、検索入力に文字を打てる
- [ ] `Escape` で非表示になる
- [ ] ウィンドウを閉じても終了せず、トレイに残る
- [ ] トレイの二重クリックで表示される
- [ ] トレイメニューの終了で、関連プロセスが残らずに終了する
- [ ] 二重に起動しようとすると、既存のウィンドウが表示されて新しいプロセスが終わる
- [ ] ホットキーが他アプリと衝突している場合、その旨が表示され、トレイからは使える
- [ ] 非表示のあいだ、CPU 使用率が 0% と見なせる水準にある

### 4.2 Shelf と Item

- [ ] Shelf を作成、改名、削除できる。削除は配下ごと消え、事前に確認が出る
- [ ] Shelf を別の親へ移動でき、自分自身と自分の子孫へは移動できない
- [ ] ピン留めした Shelf が同一階層の先頭に並ぶ
- [ ] エクスプローラからのドロップでファイルとフォルダが追加される
- [ ] 複数ファイルをまとめて落として、一度に追加できる
- [ ] URL を追加でき、`http` と `https` 以外は拒否される
- [ ] 同一 Shelf に同じ参照を追加すると重複として拒否される
- [ ] Item の改名、メモ編集、別 Shelf への移動、削除ができる
- [ ] 削除しても参照先の実体が残っている
- [ ] ドラッグで Shelf と Item を並び替えられ、再起動後も順序が保たれる

### 4.3 起動と検索

- [ ] 二重クリックと `Enter` で Item が起動し、ウィンドウが隠れる
- [ ] ホットキーの修飾キーを押したまま起動すると、ウィンドウが表示されたまま残る
- [ ] 親フォルダを開く操作で、対象が選択された状態のエクスプローラが開く。URL では選べない
- [ ] 検索が入力に追従し、表示名、パス、メモ、Shelf 名に一致する
- [ ] **検索ボックスで日本語を入力でき、変換候補が入力位置に出る**
- [ ] `box:`、`type:`、`in:` が仕様どおり効く
- [ ] `type:` に不正な値を入れても検索が壊れない
- [ ] 検索中に Shelf を選ぶと通常モードに戻り、その Shelf の内容が出る（仕様変更 5 番）
- [ ] 参照先が存在しない Item に警告が付き、欠損一覧に出る
- [ ] 最近一覧が起動順に並び、表示件数の設定が効く

### 4.4 データ

- [ ] エクスポートした JSON を、現行版と再作成版の双方が読める
- [ ] 全置換インポートで既存データが置き換わる
- [ ] マージインポートで既存データが残り、重複は増えない（仕様変更 1 番）
- [ ] 日時が壊れたレコードを含む JSON を取り込んでも、残りが取り込まれる（仕様変更 2 番）
- [ ] アプリを強制終了させても、直前までの変更が失われない
- [ ] 保存ファイルを壊した状態で起動すると、バックアップから回復して退避先が知らされる

### 4.5 設定と表示

- [ ] ホットキーの変更が再起動なしで反映される
- [ ] ウィンドウサイズの変更が保存され、次の起動で復元される
- [ ] 起動時に表示しない設定が効く
- [ ] OS のライトとダークの切り替えに追従する
- [ ] Windows 11 で Mica が適用され、非対応環境でも背景が破綻しない
- [ ] 100% 以外の表示倍率でも配置が崩れない

### 4.6 実行環境

- [ ] WebView2 が無い環境で、何が足りないかと入手方法が示される
- [ ] Windows 10 と Windows 11 の両方で起動する

## 5. 計測手順

現行版と再作成版に同じ手順を適用し、比較できる形で記録する。

### 5.1 配布サイズ

release ビルドで生成した実行ファイルの大きさを測る。

```powershell
(Get-Item ./src-tauri/target/release/shelfy.exe).Length
Compress-Archive -Path ./src-tauri/target/release/shelfy.exe -DestinationPath ./shelfy.zip -Force
(Get-Item ./shelfy.zip).Length
```

現行版の実測値は、単一 exe が 182,347,711 バイト、zip が 69.7 MB である。

### 5.2 起動時間とホットキー応答

外部からの計測ではプロセス終了を待つ必要があり、常駐アプリには使えない。
計測用の構成で、アプリ自身に次の 2 区間を測らせてログへ出す。

- プロセス開始からトレイ常駐が完了するまで
- ホットキーの受信からウィンドウの表示完了まで

10 回試行し、中央値と最大値を記録する。
初回起動と 2 回目以降を分けて記録する。

### 5.3 検索応答

Shelf 100 件と Item 1,000 件を生成する計測用のテストを置く。
代表的なクエリ（フリーテキストのみ、`type:` のみ、`box:` との併用）で検索を 100 回実行し、1 回あたりの所要時間の中央値を記録する。

### 5.4 常駐メモリ

この構成では、本体のプロセスに加えて WebView の子プロセスが立つ。
親子をたどって合計を取る。

```powershell
$root = (Get-Process shelfy).Id
$all = Get-CimInstance Win32_Process
$ids = @($root)
do {
  $next = $all | Where-Object { $ids -contains $_.ParentProcessId } | Select-Object -ExpandProperty ProcessId
  $new  = $next | Where-Object { $ids -notcontains $_ }
  $ids += $new
} while ($new)
Get-Process -Id $ids | Measure-Object WorkingSet64 -Sum | Select-Object -ExpandProperty Sum
```

起動して 5 分間放置したあと、一覧表示と検索を各 10 回行い、その後の合計を記録する。
表示中と非表示中の両方で測る。

### 5.5 非表示中の CPU

非表示にして 5 分間放置し、その間の CPU 時間の増加を測る。
増加が計測誤差の範囲に収まることを確認する。

### 5.6 記録

計測結果は日付、コミット、環境（OS 版数、WebView2 の版数）とともに残し、フェーズ 7 の判定に使う。

## 6. ワークフローの変更

| 項目           | 現行                            | 変更後                                                       |
| -------------- | ------------------------------- | ------------------------------------------------------------ |
| ツールチェーン | .NET SDK（プレビュー版を許容）  | Rust の安定版を固定、Node.js をフロントエンドのビルドに使う  |
| 静的検査       | なし                            | `cargo fmt --check` と `cargo clippy`（警告をエラー扱い）    |
| テスト         | `dotnet test`                   | `cargo test` とフロントエンドの型検査                        |
| ビルド         | `dotnet build`                  | フロントエンドのビルドと `cargo build`                       |
| リリース       | 自己完結の単一ファイルを zip 化 | release ビルドの実行ファイルをそのまま資産にする             |
| サイズの監視   | なし                            | ビルド後に実行ファイルの大きさを検査し、10 MB 超で失敗させる |
| 依存の監査     | なし                            | `cargo audit` を CI に入れる                                 |

ツールチェーンの版数を固定するのは、更新でビルド結果が変わるのを避けるためである。
現行の設定はプレビュー版の SDK を許容しており、実行のたびに異なる版が入りうる。

Rust のビルドは初回が長い。
依存のキャッシュを効かせ、リリース時以外は最適化を弱めた設定でビルドする。

## 7. リスク

| リスク                              | 影響                   | 対応                                                                |
| ----------------------------------- | ---------------------- | ------------------------------------------------------------------- |
| 常駐メモリが目標を超える            | 軽量化の目的を損なう   | フェーズ 1 で判定し、対策後も超えるなら方針を相談して決め直す       |
| WebView2 が無い環境がある           | 起動できない           | 起動時に検出して案内する。Windows 10 環境で確認する                 |
| 内部の並び替えが作りにくい          | 操作性の低下           | HTML5 の仕組みを使わずポインタイベントで実装する（設計済み）        |
| 仕様の取りこぼし                    | 使えていた操作が消える | 第 3 節の 21 領域をテストとして先に置く                             |
| JSON スナップショットの書き込み失敗 | データ喪失             | 一時ファイルと原子的置き換え、バックアップ、破損時の回復            |
| 移行時のデータ欠落                  | 利用者のデータ喪失     | 交換形式を現行と共通にし、移行後に照合する                          |
| Rust と Tauri の習熟                | 進行の遅れ             | フェーズ 1 と 2 を小さく区切り、早い段階で手応えを確かめる          |
| フロントエンドの肥大                | メモリと起動の悪化     | UI ライブラリを追加せず、依存を増やすときはサイズを測ってから決める |

## 8. 完了の定義

次を全て満たした時点で再作成を完了とする。

- 第 3 節の 21 領域にテストがあり、通っている
- 第 4 節の受け入れ確認が全て通る
- [02-SPECIFICATION.md](02-SPECIFICATION.md) 第 11 節の非機能要件を、第 5 節の計測で満たす
- 既存利用者が [04-DATA_MIGRATION.md](04-DATA_MIGRATION.md) 第 5 節の手順でデータを移せる
- タグの push でリリース資産が生成され、置くだけで動く
- README とドキュメントが再作成後の内容に更新されている
